# Fail2Ban filter for blocked path access
[Definition]
failregex = ^<HOST> .* "(GET|POST|HEAD) .*(\.env|wp-admin|wp-login|phpmyadmin|\.git|cgi-bin|setup\.cgi|actuator|debug|console).* (404|403) .*$
            ^<HOST> .* "(GET|POST|HEAD) .*" (403|404) .*$
ignoreregex =
